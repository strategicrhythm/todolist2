<%= link_to "完了済み" , todolist_path,
                      :class => "have-#{@todolist.id} btn-have btn btn-warning btn-sm have",
                      :remote => true,
                      :method => :post %>